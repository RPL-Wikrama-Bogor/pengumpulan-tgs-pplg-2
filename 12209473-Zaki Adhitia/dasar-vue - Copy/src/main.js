//import './assets/main.css'//namafolder/namafile.

import { createApp } from 'vue'
import App from './App.vue' // import berfungsi untuk memanggil file .

createApp(App).mount('#app')//menjalankan function createApp.
//mount berfungsi untuk merender root component ke dalam.

//function createApp akan menerima sebuah komponen dari parameter(App) yang berupa html/css yang ada di dalam file app.vue.
